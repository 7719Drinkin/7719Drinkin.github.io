# Games home backgrounds

Place only `/games/` homepage background images here.

Example:

```text
assets/Games/Home/backgrounds/games-home-01.webp
```

Set the active homepage background in `data/games/catalog.json` under `home.heroBackground`.
